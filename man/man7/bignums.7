.TH BIGNUMS 7 "2026-09-24" "BigNums" "Miscellaneous Information Manual"
.nr HY 0
.ds AD l
.SH NAME
bignums \- fixed-capacity big integer and binary floating-point library
.SH DESCRIPTION
BigNums is a C99 library for unsigned integers and signed binary floats.
It uses little-endian arrays of 1024 32-bit limbs and verified FFT integer multiplication.
Float operations use exact integer intermediates, scaled division, and
integer square roots.
Numbers have fixed storage: at most 32768 integer or mantissa bits.
Unsigned integers range from 0 through 2^32768 \- 1.
The BigFloat exponent remains a signed 32-bit count of binary positions.
.PP
The larger storage changes the layout of BigInt and BigFloat.
Rebuild the library and its applications together when upgrading from the
256-limb version; old compiled objects are not binary-compatible.
The SONAME changes from libbignums.so.0 to libbignums.so.1.
Do not substitute the new library for the old SONAME.
.SH GETTING STARTED
On Linux with an ELF toolchain and GNU Make, run ./build.sh from the repository
root to build and install the shared library, headers, and manuals.
The default prefix is $HOME/.local; override it with PREFIX=/usr/local when
you have write access to that prefix.  The script calls man_setup.sh to install
manuals under PREFIX/share/man/man3 and man7.
Prefix-based installation does not invoke sudo.
.PP
For standard system-wide linking, run ./build.sh \-\-system.
It builds first, then uses sudo unless already root to install into /usr/local.
It registers /usr/local/lib in /etc/ld.so.conf.d/bignums.conf and runs ldconfig.
On a standard native Linux toolchain, an application can then be compiled with:
.PP
.EX
cc app.c \-lbignums \-o app
.EE
.PP
System mode requires Linux/glibc and ldconfig, and accepts no path overrides.
DESTDIR supports package staging; it is not included in runtime paths.
.PP
Include bignums.h and link an application against the installed library:
.PP
.EX
cc app.c \e
    \-I"$HOME/.local/include" \e
    \-L"$HOME/.local/lib" \e
    \-Wl,\-rpath,"$HOME/.local/lib" \e
    \-lbignums \-o app
.EE
.PP
The library filename is libbignums.so.1.0.0, with SONAME libbignums.so.1 and
relative symlinks for the SONAME and linker name libbignums.so.
The rpath controls runtime lookup; the linker search option alone does not.
The shared library records its dependency on the math library.
Applications that call math functions directly should also link with \-lm.
.PP
Alternatively, compile with both library sources from the bigNums directory:
.PP
.EX
cc \-std=c99 \-O2 program.c bigNumLibThingy.c \e
    smartFFTThingy.c \-lm \-o program
.EE
.PP
The math library must be linked after the source/object files that use it.
There is no required FFT-header inclusion order for applications using only
the number API.
.PP
From the repository root, build and run the examples and tests with:
.PP
.EX
make \-C bigNums shared
make \-C bigNums test demo
make \-C bigNums check
\&./build/demo
.EE
.PP
The default Make target builds only the shared library under build/lib.
Supply MAIN to also build a custom driver.  The test and demo targets build
executables under build that load the shared library using a relative rpath;
the test target also runs the numerical suite.
The numerical test executable returns nonzero if a check fails.
The check target runs the same C regression suites as test, including exact
multiplication oracles and injected FFT/allocation failures.
.SH API GROUPS
.TP
.B Types and representation
.BR bignums (3),
.BR BigInt (3),
.BR BigFloat (3).
.TP
.B Integer construction and conversion
.BR bigIntZero (3),
.BR int_32ToBigInt (3),
.BR bigIntFromString (3).
.TP
.B Integer arithmetic
.BR bigIntAddUInt_32 (3),
.BR bigIntMulUInt_32 (3),
.BR bigIntDivUInt32 (3),
.BR bigIntModUInt32 (3),
.BR bigIntMulFFT (3),
.BR bigIntSub (3),
.BR bigIntFactorial (3).
.TP
.B Integer comparison and bits
.BR bigIntCmp (3),
.BR bigIntGetBit (3),
.BR bigIntShiftLeft (3),
.BR bigIntShiftRight (3).
.TP
.B Float construction and representation
.BR bigFloatZero (3),
.BR bigFloatFromUint32 (3),
.BR bigFloatCopy (3),
.BR bigFloatNormalize (3),
.BR bigFloatTruncate (3).
.TP
.B Float arithmetic and comparison
.BR bigFloatAdd (3),
.BR bigFloatSub (3),
.BR bigFloatMul (3),
.BR bigFloatDiv (3),
.BR bigFloatReciprocal (3),
.BR bigFloatSqrt (3),
.BR bigFloatCmpAbs (3),
.BR bigFloatShiftLeft (3),
.BR bigFloatShiftRight (3).
.TP
.B Decimal output
.BR printBigInt (3),
.BR printBigFloat (3).
.TP
.B Underlying complex transforms
.BR fft (3),
.BR fft_arbitrary (3).
.SH EXAMPLES
Compute and print 50 factorial, then a square-root approximation:
.PP
.EX
#include <stdio.h>
#include "bignums.h"

int main(void)
{
    BigInt factorial;
    BigFloat two, root;

    if (bigIntFactorial(&factorial, 50) != 0)
        return 1;
    printBigInt(&factorial);
    putchar('\en');

    bigFloatFromUint32(&two, 2);
    if (bigFloatSqrt(&root, &two, 4) != 0)
        return 1;
    printBigFloat(&root, 6);
    putchar('\en');
    return 0;
}
.EE
.SH PRECISION
Float arithmetic rounds toward zero.  Addition, subtraction, and
multiplication retain up to 32768 significant binary bits.
Reciprocal, division, and square root round to 32 * target_limbs bits,
with target_limbs clamped to 1 through 1024.
Exponent range errors are reported instead of overflowing signed arithmetic.
There are no NaN or infinity representations, configurable rounding modes,
or decimal float input routine.
.SH DIAGNOSTICS
Most arithmetic routines return 0 on success, with INT_MAX or \-1 indicating
failure.  Comparisons, bit lookup, and scalar division and modulo return
values instead; consult the individual function pages.
Integer scalar arithmetic and parsing may leave partial results on failure.
Float arithmetic leaves outputs unchanged on error.
The void float truncation and printing routines report range errors by
setting errno to ERANGE.
.SH BUGS
The library remains experimental and does not validate number pointers,
size fields, or signs.  Standalone FFT transforms use double precision;
bigIntMulFFT checks every FFT coefficient with an exact modular convolution
and recovers from floating-point reconstruction errors without quadratic work.
The C regression suite includes full-capacity products, carries, reciprocal
and square-root bit patterns, and standalone FFT checks.  Testing is not an
exhaustive proof for every possible input or decimal output.
Integer multiplication takes O(n log n) time, including exact verification,
and uses up to 320 KiB of temporary heap storage.
Float multiplication, division, and square root have quadratic arithmetic costs.
Some float routines use about 100 KiB of stack including callees on the
tested build; small thread stacks need adequate headroom.
The float printer supports at most 10240 decimal places and a 32768-bit
integer part.  Unsupported output sizes set errno to ERANGE without output.
Read the function pages before using boundary-sized inputs or precision.
.SH HISTORY
The project began as a side quest for Tupper's self-referential formula and
other mathematical experiments.
.SH SEE ALSO
.BR bignums (3),
.BR bigIntFactorial (3),
.BR bigFloatSqrt (3),
.BR printBigFloat (3)
